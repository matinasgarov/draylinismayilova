# i18n (en/az/de) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add English / Azerbaijani / German i18n to Dr. Aylin Ismayilova's Next.js 16 static-export site via `app/[lang]/` routing and dictionary JSON files.

**Architecture:** All pages move under `app/[lang]/`; a root `app/layout.tsx` shell wraps the tree and `app/[lang]/layout.tsx` owns the real `<html lang>` document. Dictionary JSON files are loaded server-side by `getDictionary()` and threaded down to every component as a typed `dict` prop. The root `/` route redirects to `/en`.

**Tech Stack:** Next.js 16 (static export, `output: 'export'`), TypeScript, React 19, `server-only`, no third-party i18n library.

---

## File Map

| Action   | Path                                    | Responsibility                                     |
|----------|-----------------------------------------|----------------------------------------------------|
| Create   | `dictionaries/en.json`                  | English strings                                    |
| Create   | `dictionaries/az.json`                  | Azerbaijani strings                                |
| Create   | `dictionaries/de.json`                  | German strings                                     |
| Create   | `app/[lang]/dictionaries.ts`            | `Locale` type, `getDictionary()`, `Dictionary` type|
| Modify   | `app/layout.tsx`                        | Bare HTML shell (required by Next.js)              |
| Create   | `app/[lang]/layout.tsx`                 | Full document shell with `lang` attr & fonts       |
| Modify   | `app/page.tsx`                          | Redirect `/` → `/en`                              |
| Create   | `app/[lang]/page.tsx`                   | Homepage, threads dict to all sections             |
| Delete   | `app/blog/`                             | Replaced by `app/[lang]/blog/`                    |
| Create   | `app/[lang]/blog/[slug]/page.tsx`       | Blog post detail, locale-aware date format         |
| Modify   | `components/Nav.tsx`                    | Accept `dict`/`lang` props; add lang switcher      |
| Modify   | `components/Hero.tsx`                   | Accept `dict` prop                                 |
| Modify   | `components/About.tsx`                  | Accept `dict` prop                                 |
| Modify   | `components/Experience.tsx`             | Accept `dict` prop                                 |
| Modify   | `components/Research.tsx`               | Accept `dict` prop                                 |
| Modify   | `components/Journal.tsx`                | Accept `dict`/`lang` props; locale date            |
| Modify   | `components/Footer.tsx`                 | Accept `dict` prop                                 |
| Modify   | `app/globals.css`                       | Append `.lang-switcher` CSS                        |

---

## Task 1: Create dictionary JSON files

**Files:**
- Create: `dictionaries/en.json`
- Create: `dictionaries/az.json`
- Create: `dictionaries/de.json`

- [ ] **Step 1: Create `dictionaries/en.json`**

```json
{
  "lang": "en",
  "nav": {
    "logo": "Dr. Aylin Ismayilova",
    "about": "About",
    "experience": "Experience",
    "research": "Research",
    "journal": "Journal",
    "contact": "Contact"
  },
  "hero": {
    "eyebrow": "General Surgeon",
    "tagline1": "Precision in the operating room.",
    "tagline2": "Clarity in every consultation.",
    "tagline3": "Dedication to every patient.",
    "cta": "Read more",
    "scroll": "Scroll"
  },
  "about": {
    "sectionNum": "01",
    "sectionTitle": "About",
    "pullQuote": "Surgery is not just technique — it is trust,",
    "pullQuoteAccent": "translated into touch.",
    "bio1": "Dr. Aylin Ismayilova is a general surgeon committed to advancing the practice of surgery through both technical excellence and compassionate care. With a background in evidence-based medicine and a passion for patient outcomes, she bridges cutting-edge surgical techniques with human-centered medicine.",
    "bio2": "Her academic work spans minimally invasive procedures, post-operative recovery optimization, and surgical education. She believes that the best surgeons are perpetual students — of medicine, of their patients, and of themselves.",
    "bio3": "Beyond the operating room, Dr. Ismayilova writes on the intersection of medicine and modern life, making complex clinical realities accessible to wider audiences.",
    "tags": ["General Surgery", "Laparoscopy", "Emergency Surgery", "Surgical Research", "Medical Writing"]
  },
  "experience": {
    "sectionNum": "02",
    "sectionTitle": "Experience & Education"
  },
  "research": {
    "sectionNum": "03",
    "sectionTitle": "Research & Publications"
  },
  "journal": {
    "sectionNum": "04",
    "sectionTitle": "Journal",
    "readEssay": "Read essay →"
  },
  "footer": {
    "specialtyTitle": "Specialty",
    "specialty1": "General Surgery",
    "specialty2": "Minimally Invasive Procedures",
    "specialty3": "Emergency Surgery",
    "contactTitle": "Contact",
    "locationTitle": "Location",
    "location1": "Baku, Azerbaijan",
    "location2": "Available for academic collaborations worldwide",
    "copyright": "© 2026 Dr. Aylin Ismayilova. All rights reserved.",
    "tagline": "General Surgeon · Researcher · Writer"
  },
  "blog": {
    "back": "← Back to Journal"
  }
}
```

- [ ] **Step 2: Create `dictionaries/az.json`**

```json
{
  "lang": "az",
  "nav": {
    "logo": "Dr. Aylin İsmayılova",
    "about": "Haqqında",
    "experience": "Təcrübə",
    "research": "Tədqiqat",
    "journal": "Jurnal",
    "contact": "Əlaqə"
  },
  "hero": {
    "eyebrow": "Ümumi Cərrah",
    "tagline1": "Əməliyyat otağında dəqiqlik.",
    "tagline2": "Hər konsultasiyada aydınlıq.",
    "tagline3": "Hər xəstəyə sədaqət.",
    "cta": "Daha çox",
    "scroll": "Sürüşdür"
  },
  "about": {
    "sectionNum": "01",
    "sectionTitle": "Haqqında",
    "pullQuote": "Cərrahlıq yalnız texnika deyil — bu, etimadd ır,",
    "pullQuoteAccent": "toxunuşa çevrilmiş.",
    "bio1": "Dr. Aylin İsmayılova həm texniki mükəmməllik, həm də şəfqətli qayğı vasitəsilə cərrahlıq praktikasını inkişaf etdirməyə sadiq ümumi bir cərrahdır. Sübutlara əsaslanan tibb sahəsindəki təcrübəsi və xəstə nəticələrinə olan həvəsi ilə o, müasir cərrahi texnikaları insana yönümlü tibb ilə birləşdirir.",
    "bio2": "Onun akademik işi minimal invaziv prosedurları, əməliyyatdan sonrakı sağalmanın optimallaşdırılmasını və cərrahi təhsili əhatə edir. O inanır ki, ən yaxşı cərrahlar həmişə tələbədir — tibb, xəstələri və özləri barədə.",
    "bio3": "Əməliyyat otağından kənarda Dr. İsmayılova tibb və müasir həyatın kəsişməsi haqqında yazır, mürəkkəb klinik reallıqları geniş auditoriyaya çatdırır.",
    "tags": ["Ümumi Cərrahlıq", "Laparoskopiya", "Təcili Cərrahlıq", "Cərrahi Tədqiqat", "Tibbi Yazı"]
  },
  "experience": {
    "sectionNum": "02",
    "sectionTitle": "Təcrübə & Təhsil"
  },
  "research": {
    "sectionNum": "03",
    "sectionTitle": "Tədqiqat & Nəşrlər"
  },
  "journal": {
    "sectionNum": "04",
    "sectionTitle": "Jurnal",
    "readEssay": "Esseni oxu →"
  },
  "footer": {
    "specialtyTitle": "İxtisas",
    "specialty1": "Ümumi Cərrahlıq",
    "specialty2": "Minimal İnvaziv Prosedurlar",
    "specialty3": "Təcili Cərrahlıq",
    "contactTitle": "Əlaqə",
    "locationTitle": "Yer",
    "location1": "Bakı, Azərbaycan",
    "location2": "Dünya üzrə akademik əməkdaşlıqlara açıqdır",
    "copyright": "© 2026 Dr. Aylin İsmayılova. Bütün hüquqlar qorunur.",
    "tagline": "Ümumi Cərrah · Tədqiqatçı · Yazıçı"
  },
  "blog": {
    "back": "← Jurnala qayıt"
  }
}
```

- [ ] **Step 3: Create `dictionaries/de.json`**

```json
{
  "lang": "de",
  "nav": {
    "logo": "Dr. Aylin Ismayilova",
    "about": "Über mich",
    "experience": "Erfahrung",
    "research": "Forschung",
    "journal": "Journal",
    "contact": "Kontakt"
  },
  "hero": {
    "eyebrow": "Allgemeinchirurgin",
    "tagline1": "Präzision im Operationssaal.",
    "tagline2": "Klarheit in jeder Konsultation.",
    "tagline3": "Hingabe für jeden Patienten.",
    "cta": "Mehr lesen",
    "scroll": "Scrollen"
  },
  "about": {
    "sectionNum": "01",
    "sectionTitle": "Über mich",
    "pullQuote": "Chirurgie ist nicht nur Technik — sie ist Vertrauen,",
    "pullQuoteAccent": "übersetzt in Berührung.",
    "bio1": "Dr. Aylin Ismayilova ist eine Allgemeinchirurgin, die sich der Weiterentwicklung der Chirurgie durch technische Exzellenz und einfühlsame Fürsorge widmet. Mit einem Hintergrund in der evidenzbasierten Medizin und einer Leidenschaft für Patientenergebnisse verbindet sie modernste chirurgische Techniken mit menschenzentrierter Medizin.",
    "bio2": "Ihre akademische Arbeit umfasst minimalinvasive Verfahren, die Optimierung der postoperativen Erholung und die chirurgische Ausbildung. Sie glaubt, dass die besten Chirurgen ewige Schüler sind — der Medizin, ihrer Patienten und sich selbst gegenüber.",
    "bio3": "Außerhalb des Operationssaals schreibt Dr. Ismayilova über die Schnittstelle von Medizin und modernem Leben und macht komplexe klinische Realitäten einem breiteren Publikum zugänglich.",
    "tags": ["Allgemeinchirurgie", "Laparoskopie", "Notfallchirurgie", "Chirurgische Forschung", "Medizinisches Schreiben"]
  },
  "experience": {
    "sectionNum": "02",
    "sectionTitle": "Erfahrung & Ausbildung"
  },
  "research": {
    "sectionNum": "03",
    "sectionTitle": "Forschung & Publikationen"
  },
  "journal": {
    "sectionNum": "04",
    "sectionTitle": "Journal",
    "readEssay": "Essay lesen →"
  },
  "footer": {
    "specialtyTitle": "Fachgebiet",
    "specialty1": "Allgemeinchirurgie",
    "specialty2": "Minimalinvasive Verfahren",
    "specialty3": "Notfallchirurgie",
    "contactTitle": "Kontakt",
    "locationTitle": "Standort",
    "location1": "Baku, Aserbaidschan",
    "location2": "Offen für akademische Kooperationen weltweit",
    "copyright": "© 2026 Dr. Aylin Ismayilova. Alle Rechte vorbehalten.",
    "tagline": "Allgemeinchirurgin · Forscherin · Autorin"
  },
  "blog": {
    "back": "← Zurück zum Journal"
  }
}
```

- [ ] **Step 4: Commit**

```powershell
git add dictionaries/en.json dictionaries/az.json dictionaries/de.json
git commit -m "feat: add i18n dictionary files (en/az/de)"
```

---

## Task 2: Create `app/[lang]/dictionaries.ts`

**Files:**
- Create: `app/[lang]/dictionaries.ts`

- [ ] **Step 1: Create the file**

```typescript
import 'server-only'

export type Locale = 'en' | 'az' | 'de'
export const locales: Locale[] = ['en', 'az', 'de']
export const defaultLocale: Locale = 'en'

export function hasLocale(lang: string): lang is Locale {
  return locales.includes(lang as Locale)
}

export async function getDictionary(locale: Locale) {
  const dicts = {
    en: () => import('../../dictionaries/en.json').then((m) => m.default),
    az: () => import('../../dictionaries/az.json').then((m) => m.default),
    de: () => import('../../dictionaries/de.json').then((m) => m.default),
  }
  return dicts[locale]()
}

export type Dictionary = Awaited<ReturnType<typeof getDictionary>>
```

- [ ] **Step 2: Commit**

```powershell
git add "app/[lang]/dictionaries.ts"
git commit -m "feat: add getDictionary and Dictionary type"
```

---

## Task 3: Restructure root layout and add `app/[lang]/layout.tsx`

**Files:**
- Modify: `app/layout.tsx` (bare shell)
- Create: `app/[lang]/layout.tsx`

- [ ] **Step 1: Replace `app/layout.tsx` with a bare shell**

The root layout must exist (Next.js requirement) but must NOT render `<html>/<body>` because `app/[lang]/layout.tsx` will do that. Replace the entire file:

```tsx
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html>
      <body>{children}</body>
    </html>
  )
}
```

Note: This bare `<html>/<body>` is needed only to satisfy Next.js's requirement that the root layout render a full document. The `app/[lang]/layout.tsx` nested layout will override `lang` and inject proper head content.

- [ ] **Step 2: Create `app/[lang]/layout.tsx`**

```tsx
import type { Metadata } from 'next'
import '../globals.css'
import { locales, hasLocale, type Locale } from './dictionaries'

export const metadata: Metadata = {
  title: 'Dr. Aylin Ismayilova — General Surgeon',
  description:
    'Personal website and blog of Dr. Aylin Ismayilova, General Surgeon based in Baku, Azerbaijan.',
}

export async function generateStaticParams() {
  return locales.map((lang) => ({ lang }))
}

export default async function LangLayout({
  children,
  params,
}: {
  children: React.ReactNode
  params: Promise<{ lang: string }>
}) {
  const { lang } = await params
  const locale: Locale = hasLocale(lang) ? lang : 'en'

  return (
    <html lang={locale}>
      <head>
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossOrigin="anonymous" />
        <link
          href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;0,600;1,300;1,400;1,500&family=DM+Sans:ital,opsz,wght@0,9..40,300;0,9..40,400;0,9..40,500;1,9..40,300&display=swap"
          rel="stylesheet"
        />
      </head>
      <body>{children}</body>
    </html>
  )
}
```

- [ ] **Step 3: Commit**

```powershell
git add app/layout.tsx "app/[lang]/layout.tsx"
git commit -m "feat: restructure layouts for [lang] routing"
```

---

## Task 4: Replace `app/page.tsx` with a redirect

**Files:**
- Modify: `app/page.tsx`

- [ ] **Step 1: Replace `app/page.tsx`**

```tsx
import { redirect } from 'next/navigation'

export default function Root() {
  redirect('/en')
}
```

- [ ] **Step 2: Commit**

```powershell
git add app/page.tsx
git commit -m "feat: redirect root / to /en"
```

---

## Task 5: Create `app/[lang]/page.tsx` (homepage)

**Files:**
- Create: `app/[lang]/page.tsx`

- [ ] **Step 1: Create the file**

```tsx
import { notFound } from 'next/navigation'
import Nav from '@/components/Nav'
import ScrollRevealInit from '@/components/ScrollRevealInit'
import Hero from '@/components/Hero'
import About from '@/components/About'
import Experience from '@/components/Experience'
import Research from '@/components/Research'
import Journal from '@/components/Journal'
import Footer from '@/components/Footer'
import { getPosts } from '@/lib/posts'
import { getDictionary, hasLocale } from './dictionaries'

export default async function Home({ params }: { params: Promise<{ lang: string }> }) {
  const { lang } = await params
  if (!hasLocale(lang)) notFound()

  const dict = await getDictionary(lang)
  const posts = getPosts()

  return (
    <>
      <Nav dict={dict} lang={lang} />
      <ScrollRevealInit />
      <Hero dict={dict} />
      <About dict={dict} />
      <Experience dict={dict} />
      <Research dict={dict} />
      <Journal posts={posts} dict={dict} />
      <Footer dict={dict} />
    </>
  )
}
```

- [ ] **Step 2: Commit**

```powershell
git add "app/[lang]/page.tsx"
git commit -m "feat: add localized homepage at app/[lang]/page.tsx"
```

---

## Task 6: Create `app/[lang]/blog/[slug]/page.tsx` and delete old blog route

**Files:**
- Create: `app/[lang]/blog/[slug]/page.tsx`
- Delete: `app/blog/` (entire directory)

- [ ] **Step 1: Create `app/[lang]/blog/[slug]/page.tsx`**

```tsx
import { notFound } from 'next/navigation'
import Nav from '@/components/Nav'
import { getPost, getPosts } from '@/lib/posts'
import { getDictionary, hasLocale, locales } from '../../dictionaries'

export async function generateStaticParams() {
  const posts = getPosts()
  const params = []
  for (const lang of locales) {
    for (const post of posts) {
      params.push({ lang, slug: post.slug })
    }
  }
  return params
}

function formatDate(dateStr: string, lang: string): string {
  const locale = lang === 'az' ? 'az-AZ' : lang === 'de' ? 'de-DE' : 'en-US'
  return new Date(dateStr).toLocaleDateString(locale, {
    month: 'long',
    day: 'numeric',
    year: 'numeric',
  })
}

export default async function BlogPost({
  params,
}: {
  params: Promise<{ lang: string; slug: string }>
}) {
  const { lang, slug } = await params
  if (!hasLocale(lang)) notFound()

  const dict = await getDictionary(lang)
  const post = await getPost(slug)

  return (
    <>
      <Nav dict={dict} lang={lang} />
      <main className="blog-detail">
        <a href={`/${lang}/#blog`} className="blog-detail-back">
          {dict.blog.back}
        </a>
        <div className="blog-detail-meta">
          <span>{formatDate(post.date, lang)}</span>
          <span className="dot" />
          <span>{post.category}</span>
        </div>
        <h1>{post.title}</h1>
        <div
          className="blog-detail-body"
          dangerouslySetInnerHTML={{ __html: post.contentHtml }}
        />
      </main>
    </>
  )
}
```

- [ ] **Step 2: Delete the old blog directory**

```powershell
Remove-Item -Recurse -Force "app\blog"
```

- [ ] **Step 3: Commit**

```powershell
git add "app/[lang]/blog/[slug]/page.tsx"
git rm -r app/blog
git commit -m "feat: move blog detail under app/[lang]/blog/[slug]"
```

---

## Task 7: Update `components/Nav.tsx`

**Files:**
- Modify: `components/Nav.tsx`

- [ ] **Step 1: Replace the entire file**

```tsx
'use client'

import { useEffect, useRef } from 'react'
import type { Dictionary } from '@/app/[lang]/dictionaries'

interface NavProps {
  dict: Dictionary
  lang: string
}

export default function Nav({ dict, lang }: NavProps) {
  const navRef = useRef<HTMLElement>(null)

  useEffect(() => {
    const nav = navRef.current
    if (!nav) return
    const handler = () => nav.classList.toggle('scrolled', window.scrollY > 40)
    window.addEventListener('scroll', handler, { passive: true })
    return () => window.removeEventListener('scroll', handler)
  }, [])

  const otherLangs = ['en', 'az', 'de'].filter((l) => l !== lang)

  return (
    <nav id="nav" ref={navRef}>
      <a href={`/${lang}/#home`} className="nav-logo">
        {dict.nav.logo}
      </a>
      <ul className="nav-links">
        <li><a href={`/${lang}/#about`}>{dict.nav.about}</a></li>
        <li><a href={`/${lang}/#experience`}>{dict.nav.experience}</a></li>
        <li><a href={`/${lang}/#research`}>{dict.nav.research}</a></li>
        <li><a href={`/${lang}/#blog`}>{dict.nav.journal}</a></li>
        <li><a href={`/${lang}/#contact`}>{dict.nav.contact}</a></li>
      </ul>
      <div className="lang-switcher">
        <span className="lang-current">{lang.toUpperCase()}</span>
        <div className="lang-dropdown">
          {otherLangs.map((l) => (
            <a key={l} href={`/${l}/`} className="lang-option">
              {l.toUpperCase()}
            </a>
          ))}
        </div>
      </div>
    </nav>
  )
}
```

- [ ] **Step 2: Commit**

```powershell
git add components/Nav.tsx
git commit -m "feat: add dict/lang props and language switcher to Nav"
```

---

## Task 8: Update `components/Hero.tsx`

**Files:**
- Modify: `components/Hero.tsx`

- [ ] **Step 1: Replace the entire file**

```tsx
import type { Dictionary } from '@/app/[lang]/dictionaries'

export default function Hero({ dict }: { dict: Dictionary }) {
  return (
    <section className="hero" id="home">
      <div className="hero-left">
        <div className="hero-eyebrow">{dict.hero.eyebrow}</div>
        <h1 className="hero-name">
          <em>Aylin</em>
          <span className="last">Ismayilova</span>
        </h1>
        <p className="hero-tagline">
          {dict.hero.tagline1}
          <br />
          {dict.hero.tagline2}
          <br />
          {dict.hero.tagline3}
        </p>
        <div className="hero-cta-row">
          <a href="#about" className="btn-outline">
            {dict.hero.cta} →
          </a>
          <div className="scroll-hint">
            <div className="bar" />
            <span>{dict.hero.scroll}</span>
          </div>
        </div>
      </div>
      <div className="hero-right">
        <img
          src="/uploads/photo-1781454037417.jpeg"
          alt="Dr. Aylin Ismayilova, General Surgeon"
        />
      </div>
    </section>
  )
}
```

- [ ] **Step 2: Commit**

```powershell
git add components/Hero.tsx
git commit -m "feat: thread dict prop into Hero"
```

---

## Task 9: Update `components/About.tsx`

**Files:**
- Modify: `components/About.tsx`

- [ ] **Step 1: Replace the entire file**

```tsx
import type { Dictionary } from '@/app/[lang]/dictionaries'

export default function About({ dict }: { dict: Dictionary }) {
  return (
    <section className="sec sec-alt" id="about">
      <div className="sec-head">
        <span className="sec-num">{dict.about.sectionNum}</span>
        <h2 className="sec-title">{dict.about.sectionTitle}</h2>
      </div>
      <div className="about-grid">
        <div className="reveal">
          <p className="about-pull">
            &ldquo;{dict.about.pullQuote}{' '}
            <span className="terra">{dict.about.pullQuoteAccent}&rdquo;</span>
          </p>
        </div>
        <div className="about-bio reveal reveal-delay-1">
          <p>{dict.about.bio1}</p>
          <p>{dict.about.bio2}</p>
          <p>{dict.about.bio3}</p>
          <div className="about-tags">
            {dict.about.tags.map((tag: string) => (
              <span key={tag} className="tag">{tag}</span>
            ))}
          </div>
        </div>
      </div>
    </section>
  )
}
```

- [ ] **Step 2: Commit**

```powershell
git add components/About.tsx
git commit -m "feat: thread dict prop into About"
```

---

## Task 10: Update `components/Experience.tsx`

**Files:**
- Modify: `components/Experience.tsx`

- [ ] **Step 1: Replace the entire file**

```tsx
import experienceData from '@/content/experience.json'
import type { Dictionary } from '@/app/[lang]/dictionaries'

interface ExperienceEntry {
  years: string
  role: string
  place: string
  description: string
}

export default function Experience({ dict }: { dict: Dictionary }) {
  const entries = experienceData as ExperienceEntry[]

  return (
    <section className="sec" id="experience">
      <div className="sec-head">
        <span className="sec-num">{dict.experience.sectionNum}</span>
        <h2 className="sec-title">{dict.experience.sectionTitle}</h2>
      </div>
      {entries.map((entry, i) => (
        <div key={i} className="timeline-item reveal">
          <div className="t-year" style={{ whiteSpace: 'pre-line' }}>
            {entry.years}
          </div>
          <div className="t-body">
            <h3>{entry.role}</h3>
            <div className="t-place">{entry.place}</div>
            <p>{entry.description}</p>
          </div>
        </div>
      ))}
    </section>
  )
}
```

- [ ] **Step 2: Commit**

```powershell
git add components/Experience.tsx
git commit -m "feat: thread dict prop into Experience"
```

---

## Task 11: Update `components/Research.tsx`

**Files:**
- Modify: `components/Research.tsx`

- [ ] **Step 1: Replace the entire file**

```tsx
import publicationsData from '@/content/publications.json'
import type { Dictionary } from '@/app/[lang]/dictionaries'

interface Publication {
  year: string
  title: string
  journal: string
}

export default function Research({ dict }: { dict: Dictionary }) {
  const publications = publicationsData as Publication[]

  return (
    <section className="sec sec-alt" id="research">
      <div className="sec-head">
        <span className="sec-num">{dict.research.sectionNum}</span>
        <h2 className="sec-title">{dict.research.sectionTitle}</h2>
      </div>
      {publications.map((pub, i) => (
        <div key={i} className="pub-item reveal">
          <div className="p-year">{pub.year}</div>
          <div className="p-body">
            <h3>{pub.title}</h3>
            <div className="p-journal">{pub.journal}</div>
          </div>
          <div className="pub-arr">→</div>
        </div>
      ))}
    </section>
  )
}
```

- [ ] **Step 2: Commit**

```powershell
git add components/Research.tsx
git commit -m "feat: thread dict prop into Research"
```

---

## Task 12: Update `components/Journal.tsx`

**Files:**
- Modify: `components/Journal.tsx`

- [ ] **Step 1: Replace the entire file**

```tsx
import Link from 'next/link'
import type { PostMeta } from '@/lib/posts'
import type { Dictionary } from '@/app/[lang]/dictionaries'

interface JournalProps {
  posts: PostMeta[]
  dict: Dictionary
}

function formatDate(dateStr: string, lang: string): string {
  const locale = lang === 'az' ? 'az-AZ' : lang === 'de' ? 'de-DE' : 'en-US'
  return new Date(dateStr).toLocaleDateString(locale, { month: 'long', year: 'numeric' })
}

export default function Journal({ posts, dict }: JournalProps) {
  const lang = dict.lang
  return (
    <section className="sec" id="blog">
      <div className="sec-head">
        <span className="sec-num">{dict.journal.sectionNum}</span>
        <h2 className="sec-title">{dict.journal.sectionTitle}</h2>
      </div>
      <div className="blog-grid">
        {posts.map((post, i) => (
          <Link
            key={post.slug}
            href={`/${lang}/blog/${post.slug}`}
            className={`blog-card reveal${i > 0 ? ` reveal-delay-${i}` : ''}`}
          >
            <div className="b-meta">
              <span>{formatDate(post.date, lang)}</span>
              <span className="dot" />
              <span>{post.category}</span>
            </div>
            <h3>{post.title}</h3>
            <p>{post.excerpt}</p>
            <span className="b-link">{dict.journal.readEssay}</span>
          </Link>
        ))}
      </div>
    </section>
  )
}
```

- [ ] **Step 2: Commit**

```powershell
git add components/Journal.tsx
git commit -m "feat: thread dict prop into Journal, locale-aware dates"
```

---

## Task 13: Update `components/Footer.tsx`

**Files:**
- Modify: `components/Footer.tsx`

- [ ] **Step 1: Replace the entire file**

```tsx
import type { Dictionary } from '@/app/[lang]/dictionaries'

export default function Footer({ dict }: { dict: Dictionary }) {
  return (
    <footer id="contact">
      <div className="footer-name">
        <em>Aylin</em>
        <br />
        Ismayilova
      </div>
      <div className="footer-cols">
        <div className="f-col">
          <h4>{dict.footer.specialtyTitle}</h4>
          <p>{dict.footer.specialty1}</p>
          <p>{dict.footer.specialty2}</p>
          <p>{dict.footer.specialty3}</p>
        </div>
        <div className="f-col">
          <h4>{dict.footer.contactTitle}</h4>
          <a href="mailto:dr.aylin@email.com">dr.aylin@email.com</a>
          <a href="#">LinkedIn</a>
          <a href="#">ResearchGate</a>
          <a href="#">Google Scholar</a>
        </div>
        <div className="f-col">
          <h4>{dict.footer.locationTitle}</h4>
          <p>{dict.footer.location1}</p>
          <p>{dict.footer.location2}</p>
        </div>
      </div>
      <div className="footer-copy">
        <span>{dict.footer.copyright}</span>
        <span>{dict.footer.tagline}</span>
      </div>
    </footer>
  )
}
```

- [ ] **Step 2: Commit**

```powershell
git add components/Footer.tsx
git commit -m "feat: thread dict prop into Footer"
```

---

## Task 14: Add language switcher CSS to `app/globals.css`

**Files:**
- Modify: `app/globals.css` (append to end of file)

- [ ] **Step 1: Append CSS to end of `app/globals.css`**

Add these lines at the very end of the file:

```css
/* ── LANGUAGE SWITCHER ───────────────────────────────── */
.lang-switcher {
  position: relative;
  margin-left: 2rem;
}
.lang-current {
  font-size: 0.68rem; letter-spacing: 0.12em;
  text-transform: uppercase; color: var(--mid);
  cursor: pointer;
  padding: 0.3rem 0.5rem;
  border: 1px solid var(--rule);
  transition: color 0.2s, border-color 0.2s;
}
.lang-switcher:hover .lang-current {
  color: var(--dark); border-color: var(--mid);
}
.lang-dropdown {
  display: none;
  position: absolute; top: calc(100% + 0.5rem); right: 0;
  background: var(--cream);
  border: 1px solid var(--rule);
  min-width: 3.5rem;
  z-index: 300;
}
.lang-switcher:hover .lang-dropdown { display: block; }
.lang-option {
  display: block;
  font-size: 0.68rem; letter-spacing: 0.12em;
  text-transform: uppercase; color: var(--mid);
  padding: 0.5rem 0.75rem;
  transition: background 0.15s, color 0.15s;
}
.lang-option:hover { background: var(--sand); color: var(--dark); }
```

- [ ] **Step 2: Commit**

```powershell
git add app/globals.css
git commit -m "feat: add language switcher CSS"
```

---

## Task 15: Build and verify

- [ ] **Step 1: Run the build**

```powershell
npm run build
```

Expected exit code: `0`

Expected output in `out/`:
- `out/en/index.html`
- `out/az/index.html`
- `out/de/index.html`
- Blog posts under `out/en/blog/`, `out/az/blog/`, `out/de/blog/`

- [ ] **Step 2: If build fails, triage by error type**

**TypeScript error "Property X does not exist on type Dictionary"**
- The `Dictionary` type is inferred from `en.json`. Confirm all keys match across all three JSON files.

**Error: Cannot find module '../../dictionaries/en.json'"**
- Confirm TypeScript `compilerOptions` has `"resolveJsonModule": true` in `tsconfig.json`. If missing, add it.

**Error about `params` type**
- Ensure `params: Promise<{ lang: string }>` (not plain object) in all page/layout files — this is required in Next.js 16.

**Error about duplicate `<html>` nesting**
- In `app/layout.tsx` the bare `<html><body>{children}</body></html>` is intentional; Next.js merges nested layout `<html>` tags in static export. If the build still complains, change `app/layout.tsx` to just `export default function RootLayout({ children }: { children: React.ReactNode }) { return <>{children}</> }` — but this may break the Next.js requirement for a root document element; test both.

- [ ] **Step 3: Commit final build confirmation**

```powershell
git add .
git commit -m "feat: add i18n support (en/az/de) with language switcher"
```
